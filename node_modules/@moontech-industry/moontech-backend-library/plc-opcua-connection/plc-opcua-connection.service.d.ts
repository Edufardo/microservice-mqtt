import { ClientSession } from 'node-opcua-client';
import { NodeOperationsService } from '../node-operations/node-operations.service';
export declare class PlcOpcuaConnectionService {
    private options;
    private readonly nodeOperationsService;
    private client;
    private session;
    subscriptionCrosses: any;
    private endpointUrl;
    constructor(options: any, nodeOperationsService: NodeOperationsService);
    timeout(ms: number): Promise<unknown>;
    getSession(): ClientSession;
    existsSession(): boolean;
    createConnectionAndSession(): Promise<void>;
    createConnection(): Promise<void>;
    createSession(): Promise<ClientSession>;
    closeSession(): Promise<void>;
    closeConnection(): Promise<void>;
}
