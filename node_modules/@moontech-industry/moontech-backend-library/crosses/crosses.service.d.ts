import { ICross } from "../models-mongo/cross.model";
import { MongoDbService } from "../mongo-db/mongo-db.service";
import { WebsocketGateway } from "../websocket/websocket.gateway";
import { Cross } from "./dto/crosses.dto";
export declare class CrossesService {
    private readonly mongoDb;
    private readonly websocketService;
    private CrossModel;
    constructor(mongoDb: MongoDbService, websocketService: WebsocketGateway);
    findCross(crossId: string): Promise<ICross>;
    fetchCrosses(): Promise<Cross[]>;
    createCross(newCross: Cross): Promise<ICross>;
    updateCross(crossId: string, newCross: Cross): Promise<ICross>;
    deleteCross(crossId: string): Promise<ICross>;
    notifyWebsockets(): Promise<void>;
}
