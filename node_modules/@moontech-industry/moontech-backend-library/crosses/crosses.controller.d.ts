import { ICross } from '../models-mongo/cross.model';
import { CrossesService } from './crosses.service';
import { Cross } from './dto/crosses.dto';
export declare class CrossesController {
    private crossesService;
    constructor(crossesService: CrossesService);
    fetchAllUsers(): Promise<Cross[]>;
    createCross(newCross: Cross): Promise<Cross>;
    updateCross(crossId: string, newCross: Cross): Promise<ICross>;
    deleteCross(crossId: string): Promise<ICross>;
}
