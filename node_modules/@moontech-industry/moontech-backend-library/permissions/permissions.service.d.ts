import { MongoDbService } from '../mongo-db/mongo-db.service';
import { PermissionDto } from './dto/permission.dto';
import { IPermission } from '../models-mongo/permission.model';
import { WebsocketGateway } from '../websocket/websocket.gateway';
export declare class PermissionService {
    private readonly mongoDb;
    private websocketService;
    private PermissionModel;
    constructor(mongoDb: MongoDbService, websocketService: WebsocketGateway);
    populate(): Promise<void>;
    createNewPermission(newPermission: PermissionDto): Promise<PermissionDto>;
    deletePermission(permissionName: string): Promise<void>;
    updatePermission(permissionName: string, newPermission: PermissionDto): Promise<void>;
    findPermission(permissionName: string): Promise<IPermission>;
    fetchAllPermissions(): Promise<IPermission[]>;
    notifyWebsockets(): Promise<void>;
}
