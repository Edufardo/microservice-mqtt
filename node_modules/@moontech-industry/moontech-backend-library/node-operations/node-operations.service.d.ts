import { ClientSession } from 'node-opcua-client';
import { PlcVariable } from './models/PlcVariable';
export declare class NodeOperationsService {
    constructor();
    check_session(session: ClientSession): Promise<boolean>;
    getMultiplePlcVariablesValueByNodeId(session: ClientSession, nodeId: string[]): Promise<any>;
    getPlcVariableValueByNodeId(session: ClientSession, nodeId: string): Promise<PlcVariable>;
    setPlcVariableValueByNodeId(session: ClientSession, nodeId: string, nodeValue: any): Promise<boolean>;
    browseByNodeId(session: ClientSession, nodeId: string): Promise<any>;
}
