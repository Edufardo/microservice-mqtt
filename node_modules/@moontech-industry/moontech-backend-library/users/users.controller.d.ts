import { IUser } from '../models-mongo/user.model';
import { EditUserDto } from './dto/editUser.dto';
import { UserDto } from './dto/user.dto';
import { UsersService } from './users.service';
export declare class UsersController {
    private usersService;
    constructor(usersService: UsersService);
    createUser(newUser: UserDto): Promise<UserDto>;
    deleteUser(userCode: string): Promise<UserDto>;
    updateUser(userCode: string, user: EditUserDto): Promise<IUser>;
    fetchAllUsers(): Promise<UserDto[]>;
}
