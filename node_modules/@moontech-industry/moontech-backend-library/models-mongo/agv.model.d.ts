import * as mongoose from 'mongoose';
import { Document } from 'mongoose';
export interface IAgv extends Document {
    id: string;
    ip: string;
    active: boolean;
}
export declare const agvsSchema: mongoose.Schema<IAgv, mongoose.Model<IAgv, any, any>, undefined, {}>;
