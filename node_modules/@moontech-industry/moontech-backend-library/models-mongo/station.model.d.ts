import * as mongoose from 'mongoose';
import { STATUS } from '../common/status';
import { Document } from 'mongoose';
export interface IStation extends Document {
    name: string;
    coords: string;
    status: STATUS;
}
export declare const stationSchema: mongoose.Schema<IStation, mongoose.Model<IStation, any, any>, undefined, {}>;
