import * as mongoose from 'mongoose';
import { Document } from 'mongoose';
export interface ICross extends Document {
    crossId: string;
    occupied: boolean;
    waiting: string[];
    agvId?: string;
    description?: string;
    blockingTags?: number[];
    liberationTags?: number[];
}
export declare const crossSchema: mongoose.Schema<ICross, mongoose.Model<ICross, any, any>, undefined, {}>;
