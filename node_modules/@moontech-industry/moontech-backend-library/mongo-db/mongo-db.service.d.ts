import * as mongoose from 'mongoose';
import { MongoDbConfig } from './mongo-db-config';
export declare class MongoDbService {
    private options;
    private dbConnection;
    constructor(options: MongoDbConfig);
    createConnectionDB(options: MongoDbConfig): Promise<void>;
    getConnection(): mongoose.Connection & Promise<mongoose.Connection>;
}
