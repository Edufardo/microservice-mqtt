import { StationDto } from './dto/station.dto';
import { StationsService } from './stations.service';
export declare class StationsController {
    private stationsService;
    constructor(stationsService: StationsService);
    getStations(): Promise<StationDto[]>;
    createStation(station: StationDto): Promise<StationDto>;
    updateStation(station: StationDto): Promise<StationDto>;
    freeStation(name: string): Promise<boolean>;
    occupyStation(name: string): Promise<boolean>;
    updateStationById(station: StationDto, id: string): Promise<StationDto>;
    deleteStation(name: string): Promise<StationDto>;
}
