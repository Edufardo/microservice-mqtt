import { IStation } from '../models-mongo/station.model';
import { MongoDbService } from '../mongo-db/mongo-db.service';
import { WebsocketGateway } from '../websocket/websocket.gateway';
import { StationDto } from './dto/station.dto';
export declare class StationsService {
    private readonly mongoDb;
    private readonly websocketService;
    private StationModel;
    constructor(mongoDb: MongoDbService, websocketService: WebsocketGateway);
    getStations(): Promise<StationDto[]>;
    createStation(station: StationDto): Promise<IStation>;
    updateStation(station: StationDto): Promise<IStation>;
    updateStationById(station: StationDto, id: string): Promise<IStation>;
    deleteStation(name: string): Promise<IStation>;
    freeStation(name: string): Promise<boolean>;
    occupyStation(name: string): Promise<boolean>;
    private find;
    private findById;
    notifyWebsockets(): Promise<void>;
}
