export class Message {
  text: string;
  receivers: string[];
  subject: string;
  constructor(text: string, receivers: string[], subject: string) {
    this.text = text;
    this.receivers = receivers;
    this.subject = subject;
  }
}
