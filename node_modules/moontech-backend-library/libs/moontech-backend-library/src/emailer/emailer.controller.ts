import { Controller } from '@nestjs/common';
import { EmailerService } from './emailer.service';

@Controller('emailer')
export class EmailerController {
  constructor(private readonly emailerService: EmailerService) {}

  //This is a mock test message and endpoint, only for testing purposes.
  //   @Get()
  //   sendTestEmail() {
  //     const data: Message = {
  //       text: 'hello this is a test email',
  //       receivers: ['testemail@gmail.com'],
  //       subject: 'test email',
  //     };
  //     this.emailerService.sendMail(data);
  //   }
}
